spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url:
    username:
    password:
    driver-class-name: org.postgresql.Driver
  application:
    name: author-service

